@model Models.Categoria



@{
    

    
    var acao = "Cadastrar";
    if (Model.CategoriaId > 0)
    {
        acao = "Alterar";
    }


    ViewBag.Title = acao + " categoria";
    Layout = "~/Views/Shared/_FortunaLayout.cshtml";     
}





@section Breadcrumb{
      <li class="active"><span class="divider">/</span> @Html.ActionLink("Configurações", "Configuracoes", "Principal")</li>
<li class="active"><span class="divider">/</span>    @Html.ActionLink("Categorias", "Index", new { id = 0 })</li>
}


<div class="cwell">
        @using (Html.BeginForm("Criar", "Categorias", FormMethod.Post, new { @class = "form-horizontal" }))
        {
            @Html.ValidationSummary(true)

            if (Model.CategoriaId > 0)
            {
            @Html.Hidden("CategoriaId", Model.CategoriaId);
                                                          acao = "Alterar";
            }

            if (BLL.Usuarios.GetUtilizaFilial() == true && BLL.Usuarios.GetFilialIDMaster() == 0)
            { 
            <div class="control-group">
                <label class="control-label" for="ContaTipoId">@BLL.Usuarios.GetEntidadeFilial():</label>
                <div class="controls">
                    @Html.DropDownListFor(model => model.FilialID, (SelectList)ViewBag.DdlFiliais, "--Nenhuma--", new { @class = "chosen-select" })
                </div>
            </div>
            } else
                    {
                        <input type="hidden" name="FilialID" value="@BLL.Usuarios.GetFilialIDMaster()"/>
                    }
     
            <div class="control-group">
                @Html.LabelFor(model => model.CategoriaNome, new { @class = "control-label" })
                <div class="controls">
                    @Html.EditorFor(model => model.CategoriaNome)
                    @Html.ValidationMessageFor(model => model.CategoriaNome)
                </div>
            </div>
        
    
      

            <div class="form-actions">


                <input type="submit" value="@acao" class="btn btn-primary  btn-large"  TabIndex=10 />

            </div>
            
        }
    <div>
    @Html.ActionLink("Voltar", "Index")
</div>
    </div>





@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

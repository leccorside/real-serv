@model IEnumerable<Models.Feriado>

@{
    ViewBag.Title = "Feriados";
    Layout = "~/Views/Shared/_FortunaLayout.cshtml";
}

@section Breadcrumb{
    <li class="active"><span class="divider">/</span> @Html.ActionLink("Configura&ccedil;&otilde;es", "Configuracoes", "Principal")</li>
}

@section Styles {
@Html.Partial("_TableStyles")
}

<div class="ponto-container">
    <div class="ponto-header">
        <div class="ponto-title"><h2><i class="pe-7s-date"></i> Feriados</h2></div>
        @Html.ActionLink("+ Criar", "Criar", new { id = 0 }, new { @class = "btn-create" })
    </div>
    <div class="ponto-table-container">
        <table class="ponto-table" data-pagination="server">
            <thead>
                <tr>
                    <th>Data</th>
                    <th>Descri&ccedil;&atilde;o</th>
                    <th>Data de Cadastro</th>
                    <th style="width:140px">A&ccedil;&otilde;es</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td nowrap>@item.FeriadoData.ToString("dd/MM/yyyy")</td>
                        <td width="100%">@Html.DisplayFor(m => item.FeriadoDescricao)</td>
                        <td nowrap>@Html.DisplayFor(m => item.RegDtInicio)</td>
                        <td nowrap>
                            <div class="actions-cell">
                                @Html.ActionLink("Alterar", "Criar", new { id = item.FeriadoId }, new { @class = "btn-alterar" })
                                @Html.ActionLink("Excluir", "Deletar", new { id = item.FeriadoId }, new { @class = "btn-excluir" })
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="pagination">
        @Html.Paginacao((int)ViewBag.qtdPaginas)
    </div>
</div>

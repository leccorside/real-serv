@model Models.Sala


@{
    

    
    var acao = "Cadastrar";
    if (Model.SalaId > 0)
    {
        acao = "Alterar";
    }


    ViewBag.Title = acao + " Sala";
    Layout = "~/Views/Shared/_FortunaLayout.cshtml";     
}





@section Breadcrumb{
    <li class="active"><span class="divider">/</span> @Html.ActionLink("Configurações", "Configuracoes", "Principal")</li>
    <li class="active"><span class="divider">/</span> @Html.ActionLink("Salas", "Index", new { id = 0 })</li>
}


<div class="cwell">
    @using (Html.BeginForm("Criar", "Salas", FormMethod.Post, new { @class = "form-horizontal" }))
    {
        @Html.ValidationSummary(true)

        if (Model.SalaId > 0)
        {
        @Html.Hidden("SalaId", Model.SalaId);
                                            acao = "Alterar";
        }
    
        if (BLL.Usuarios.GetUtilizaFilial() == true && BLL.Usuarios.GetFilialIDMaster() == 0)
            { 
            <div class="control-group">
                <label class="control-label" for="FilialID">@BLL.Usuarios.GetEntidadeFilial():</label>
                <div class="controls">
                    @Html.DropDownListFor(model => model.FilialID, (SelectList)ViewBag.DdlFiliais, "--Nenhuma--", new { @class = "chosen-select" })
                </div>
            </div>
            } else
                    {
                        <input type="hidden" name="FilialID" value="@BLL.Usuarios.GetFilialIDMaster()"/>
                    }
            
        <div class="control-group">
            @Html.LabelFor(model => model.SalaNome, new { @class = "control-label" })
            <div class="controls">
                @Html.EditorFor(model => model.SalaNome)
                @Html.ValidationMessageFor(model => model.SalaNome)
            </div>
        </div>
        
            
        <div class="control-group">
            @Html.LabelFor(model => model.SalaCapacidade, new { @class = "control-label" })
            <div class="controls">
                

                @Html.TextBoxFor(m => m.SalaCapacidade, new {  @class = "number input-small" })
                @Html.ValidationMessageFor(model => model.SalaCapacidade)
            </div>
        </div>
        
         
            
            
                       
            

        <div class="form-actions">


            <input type="submit" value="@acao" class="btn btn-primary  btn-large"  />

        </div>
            
    }
    <div>
        @Html.ActionLink("Voltar", "Index")
    </div>
</div>






@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

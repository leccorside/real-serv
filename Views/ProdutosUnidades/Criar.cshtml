@model Models.ProdutoUnidade


@{
    

    
    var acao = "Cadastrar";
    if (Model.ProdutoUnidadeId > 0)
    {
        acao = "Alterar";
    }


    ViewBag.Title = acao + " Unidade de Medida de Produtos";
    Layout = "~/Views/Shared/_FortunaLayout.cshtml";     
}





@section Breadcrumb{
    <li class="active"><span class="divider">/</span> @Html.ActionLink("Produtos", "Produtos", "Principal")</li>
    <li class="active"><span class="divider">/</span> @Html.ActionLink("ProdutosUnidades", "Index", new { id = 0 })</li>
}


<div class="cwell">
    @using (Html.BeginForm("Criar", "ProdutosUnidades", FormMethod.Post, new { @class = "form-horizontal" }))
    {
        @Html.ValidationSummary(true)

        if (Model.ProdutoUnidadeId > 0)
        {
        @Html.Hidden("ProdutoUnidadeId", Model.ProdutoUnidadeId);
                                            acao = "Alterar";
        }
    
     
            
        <div class="control-group">
            @Html.LabelFor(model => model.ProdutoUnidadeNome, new { @class = "control-label" })
            <div class="controls">
                @Html.EditorFor(model => model.ProdutoUnidadeNome)
                @Html.ValidationMessageFor(model => model.ProdutoUnidadeNome)
            </div>
        </div>
        
            
     
            
            
                       
            

        <div class="form-actions">


            <input type="submit" value="@acao" class="btn btn-primary  btn-large"  />

        </div>
            
    }
    <div>
        @Html.ActionLink("Voltar", "Index")
    </div>
</div>






@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

@model Models.Agendamento

@{
    ViewBag.Title = "Calendario Geral";
    Layout = "~/Views/Shared/_FortunaLayout.cshtml";  

    var objEmpresas = new BLL.Empresas();
    var empresa = objEmpresas.Carregar(BLL.Usuarios.GetEmpresaId());


    var AgendaHoraIni = empresa.AgendaHoraIni;
    var AgendaHoraFim = empresa.AgendaHoraFim;




    TimeSpan span = TimeSpan.FromMinutes(empresa.AgendaIntervaloMinutos);
    string AgendaIntervaloMinutos = span.ToString(@"hh\:m");
    
    

    //DateTime x = new DateTime(DateTime.Now.Year, DateTime.Now.Month, DateTime.Now.Day, 0, AgendaIntervaloMinutos, 0);
    //AgendaHoraIni.AddHours(-1);
    //AgendaHoraFim.AddHours(+1);

}

@section Styles{
    <link href="~/Assets/plugins/fancybox/jquery.fancybox.css" rel="stylesheet" />
    <link href="~/Assets/plugins/fancybox/helpers/jquery.fancybox-buttons.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/Assets/js/fullcalendar/lib/cupertino/jquery-ui.min.css" />
    <link href="~/Assets/js/fullcalendar/fullcalendar.css" rel="stylesheet" />
    <link href="~/Assets/js/fullcalendar/fullcalendar.print.css" rel="stylesheet" media="print" />
    
    <style>
    #calendar {
        max-width: 100%;
        margin: 0 auto;
    }


    /* Responsive iFrame */
</style>
    <style>
        
        .popover
        {
            /*border: 2px dotted #000;
            
            width:300px; 


           z-index: 2060 !important;*/
            position: absolute;
            top: 0;
            left: 0;
            z-index: 16777271 !important;
            display: none;
            width: 236px;
            padding: 1px;
            text-align: left;
            white-space: normal;
            background-color: #ffffff;
            border: 1px solid #ccc;
            border: 1px solid rgba(0, 0, 0, 0.2);
            -webkit-border-radius: 6px;
            -moz-border-radius: 6px;
            border-radius: 6px;
            -webkit-box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);
            -moz-box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);
            -webkit-background-clip: padding-box;
            -moz-background-clip: padding;
            background-clip: padding-box;
        }


        /* Popover Header */
        .popover-title
        {
            background-color: #000;
            color: #eaeaea;
            font-size: 10px;
            text-align: center;
        }

        /* Popover Body */
        .popover-content
        {
            background-color: #eaeaea;
            color: #000;
            font-size: 10px;
            padding: 25px;
        }

        /* Popover Arrow */
        .arrow
        {
            border-bottom-color: black !important;
        }

        .destaque_True
        {
                background-color: black !important;
        }
    </style>

    }


<div class="row-fluid">
    <div class="span12">
        <div class="pull-right">
            @Html.DropDownList("PessoaId", (SelectList)ViewBag.DdlPessoas, new { @class = "chosen-select span10" })
        </div>

    </div>
</div>

@if (Request.Browser.IsMobileDevice)
{

    <div class="row-fluid">
        <div class="span12" style="height: auto">
            <div id='calendar' style="height: auto"></div>
        </div>

    </div>

}
else
{<div class="row-fluid">
        <div class="span12">
            <div id='calendar' style="height: auto"></div>
        </div>

    </div>

}



@section scripts{
    <link rel="stylesheet" href="~/Assets/js/fullcalendar/lib/cupertino/jquery-ui.min.css" />
    <link href="~/Assets/js/fullcalendar/fullcalendar.css" rel="stylesheet" />
    <link href="~/Assets/js/fullcalendar/fullcalendar.print.css" rel="stylesheet" media="print" />
    <script src="~/Assets/js/fullcalendar/lib/moment.min.js"></script>

    <script src="~/Assets/js/fullcalendar/fullcalendar.min.js"></script>
    <script src="~/Assets/js/fullcalendar/lang-all.js"></script>




    @if (Request.Browser.IsMobileDevice)
    {
        <!-- mobile -->
        <script>


            $("#PessoaId").change(function () {
                $('#calendar').fullCalendar('destroy');
                carregaAgenda();
            });

            function carregaAgenda(pessoaid) {
                var currentLangCode = 'pt-br';
                var pessoaid = $("#PessoaId").val();

                var url = '@Url.Content("~/Agendamento/Getevents_3/")' + pessoaid;


                function renderCalendar() {
                    $('#calendar').fullCalendar({
                        header: {
                            //left: 'prev,next today',
                            //center: 'title',
                            //right: 'month,agendaWeek,agendaDay'
                        },
                        dayClick: function (date, jsEvent, view) {

                            var r = confirm("Deseja registrar um novo agendamento no dia" + date.format("DD-MM-YYYY"));

                            if (r == true) {
                                var url = "Criar/0?origem=Agenda&dataInicio=" + date.format("DD-MM-YYYY HH:00") + "&horaInicio=" + date.format("HH:00") + "&pessoaId=@BLL.Usuarios.GetUsuarioId()";
                                parent.location = url;
                            }

                        },
                        defaultDate: moment(),
                        lang: currentLangCode,
                        buttonIcons: false, // show the prev/next text
                        weekNumbers: true,
                        editable: false,
                        eventLimit: true, // allow "more" link when too many events
                        minTime: "@AgendaHoraIni.ToString("HH:mm")",
                        maxTime: "@AgendaHoraFim.ToString("HH:mm")",
                        events: url ,
                        columnFormat: 'ddd',
                        defaultView: 'agendaDay',
                        axisFormat: 'HH:mm',
                        allDaySlot: false,
                        displayEventEnd: false,
                        weekNumbers: false,
                        handleWindowResize: true,
                        slotDuration: '@AgendaIntervaloMinutos',
                        height: '100%',
                        contentHeight: 'auto'
                    });
                }


                renderCalendar();
            }

            $(document).ready(function () {
                carregaAgenda();
            });


        </script>

    }
    else
    {
        <!-- desktop -->
        <script>

            $("#PessoaId").change(function () {
                $('#calendar').fullCalendar('destroy');
                carregaAgenda();
            });

            function carregaAgenda() {
                var currentLangCode = 'pt-br';

                var pessoaid = $("#PessoaId").val();

                var url = '@Url.Content("~/Agendamento/Getevents_3/")' + pessoaid;


                function renderCalendar() {

                    $('#calendar').fullCalendar({

                        header: {
                            left: 'prev,next today',
                            center: 'title',
                            right: 'month,agendaWeek,agendaDay'
                        },

                        eventRender: function (event, element) {
                            element.popover({
                                html: true,
                                trigger: "hover",
                                //title: event.title,
                                container: 'body',
                                placement: 'top',
                                content: event.text,
                            }).addClass('');
                        },

                        dayClick: function (date, jsEvent, view) {

                            var r = confirm("Deseja registrar um novo agendamento no dia" + date.format("DD-MM-YYYY"));

                            if (r == true) {
                                var url = "Criar/0?origem=Agenda&dataInicio=" + date.format("DD-MM-YYYY HH:00") + "&horaInicio=" + date.format("HH:00") + "&pessoaId=@BLL.Usuarios.GetUsuarioId()";
                                parent.location = url;
                            }

                        },

                        defaultDate: moment(),
                        allDaySlot: false,
                        lang: currentLangCode,
                        buttonIcons: false, // show the prev/next text
                        weekNumbers: true,
                        editable: false,
                        eventLimit: true, // allow "more" link when too many events
                        minTime: "@AgendaHoraIni.ToString("HH:mm")",
                        maxTime: "@AgendaHoraFim.ToString("HH:mm")",
                        events: url,
                        columnFormat: 'ddd',
                        defaultView: 'agendaWeek',
                        axisFormat: 'HH:mm',
                        slotDuration: '@AgendaIntervaloMinutos',
                        height: 'auto'
                    });
                }


                renderCalendar();
            }


            $(document).ready(function () {
                carregaAgenda();
            });





        </script>
    }



}



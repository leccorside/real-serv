@model IEnumerable<Models.OcorrenciaTipo>

@{
    ViewBag.Title = "Tipos de Ocorr&ecirc;ncia";
    Layout = "~/Views/Shared/_FortunaLayout.cshtml";
}

@section Breadcrumb{
    <li class="active"><span class="divider">/</span> @Html.ActionLink("Configura&ccedil;&otilde;es", "Configuracoes", "Principal")</li>
}

@section Styles {
@Html.Partial("_TableStyles")
}

<div class="ponto-container">
    <div class="ponto-header">
        <div class="ponto-title"><h2><i class="pe-7s-note2"></i> Tipos de Ocorr&ecirc;ncia</h2></div>
        @Html.ActionLink("+ Criar", "Criar", new { id = 0 }, new { @class = "btn-create" })
    </div>
    <div class="ponto-table-container">
        <table class="ponto-table">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Nome</th>
                    <th>Regra</th>
                    <th>Ocorr&ecirc;ncia</th>
                    <th style="width:140px">A&ccedil;&otilde;es</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(m => item.OcorrenciaTipoId)</td>
                        <td width="100%">@Html.DisplayFor(m => item.OcorrenciaTipoNome)</td>
                        <td>@Html.DisplayFor(m => item.OcorrenciaTipoRegra)</td>
                        <td nowrap>@Html.DisplayFor(m => item.RegDtInicio)</td>
                        <td nowrap>
                            <div class="actions-cell">
                                @Html.ActionLink("Alterar", "Criar", new { id = item.OcorrenciaTipoId }, new { @class = "btn-alterar" })
                                @Html.ActionLink("Excluir", "Deletar", new { id = item.OcorrenciaTipoId }, new { @class = "btn-excluir" })
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
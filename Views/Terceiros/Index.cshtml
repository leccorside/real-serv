@model IEnumerable<Models.Usuario>


@{
    ViewBag.Title = "Terceiros";
    Layout = "~/Views/Shared/_Layout.cshtml";
    
    
}


@section Breadcrumb{

}




@Html.ActionLink("+ Criar", "Criar", new { id = 0 }, new { @class = "btn btn-primary" })


<div class="pull-right">@Html.FormPesquisaIndex()</div>
<br /><br />
<div class="pull-right"><i class="fa fa-user-md "></i>- Engenheiro / <i class="fa fa-street-view"></i>- Fiscal</div>
<table class="table table-bordered table-striped table-highlight">
    <thead>
        <tr>
            <th nowrap>@Html.CampoOrdenacao(Bll.Configuracoes.Carregar("label.usuario.codigo"), "UsuarioCodigo")</th>
            <th>@Html.CampoOrdenacao("Nome", "UsuarioNome")</th>
            <th nowrap>@Html.CampoOrdenacao("Função", "PerfilNome")</th>
            <th><span data-toggle='tooltip' class='tip-top pull-top' data-placement='top' title='Engenheiro' data-original-title='Engenheiro'><i class="fa fa-user-md fa-lg"></i></span></th>
            <th><span data-toggle='tooltip' class='tip-top pull-top' data-placement='top' title='Fiscal' data-original-title='Fiscal'><i class="fa fa-street-view fa-lg"></i></span></th>
            <th nowrap>
                @Html.DisplayNameFor(model => model.RegDtInicio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() == 0)
        {
            <tr class="info">
                <td colspan="20"><small class="muted">Nenhum registro localizado</small></td>
            </tr>
        }
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.UsuarioCodigo</td>
                <td width="100%">
                    @item.UsuarioNome<p>
                        <small>@item.UsuarioEmail</small><br />
                        <small>@Bll.Utils.Formatacoes.FormataTelefone(item.UsuarioDDD, item.UsuarioTelefone)</small><br />
                        <small>@Bll.Utils.Formatacoes.FormataTelefone(item.UsuarioCttDDD, item.UsuarioCttTelefone)</small>
                    </p>
                </td>
                <td nowrap>
                    <small>@Html.DisplayFor(modelItem => item.PerfilNome)</small>
                </td>
                <td><i class="@(item.UsuarioEngenheiro ? "icon-ok" : "")"></i></td>
                <td><i class="@(item.UsuarioFiscal ? "icon-ok" : "")"></i></td>
                <td nowrap>
                    <small>@Html.DisplayFor(modelItem => item.RegDtInicio)</small>
                </td>
                <td nowrap>
                    <a href="~/Terceiros/Criar/@item.UsuarioId" class="btn btn-mini"><i class="fa fa-pencil"></i>Alterar</a> | 
                    @if (item.PossuiAcesso)
                    {
                        <a href="~/Terceiros/RedefinirSenha/@item.UsuarioId" class="btn btn-mini"><i class="icon-lock"></i>Redefinir Senha</a> @:|
                    }
                    else
                    {
                        <a href="#" class="btn btn-mini disabled"><i class="fa fa-ban"></i>Sem Acesso</a> @:|
	                }
                    <a href="~/Terceiros/Deletar/@item.UsuarioId" class="btn btn-mini"><i class="fa fa-times"></i>Excluir</a>
                </td>
            </tr>
        }

    </tbody>



</table>




@{
    //Controles de paginação
    int qtdPaginas = Convert.ToInt16(ViewBag.qtdPaginas);   
}
@Html.Paginacao(qtdPaginas)
